import { BankConsole } from "@/components/finance/BankConsole";
export const dynamic = "force-dynamic";
export const metadata = { title: "Bank Book & Reconciliation" };
export default function Page() { return <BankConsole />; }
