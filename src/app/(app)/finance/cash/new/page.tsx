import { CashManagementConsole } from "@/components/finance/CashManagementConsole";

// Legacy /new route — Fund Management list (Add is via the Fund Transfer page).
export default function NewCashPage() {
  return <CashManagementConsole />;
}
